<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/colorOnPrimary"
    android:id="@+id/item"
    android:orientation="horizontal">

    <com.google.android.material.card.MaterialCardView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/small_margin"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/image"
                android:layout_width="match_parent"
                android:layout_height="100dp"
                android:scaleType="centerCrop"
                tools:srcCompat="@drawable/bg_mars" />

            <TextView
                android:id="@+id/description_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="That’s it! It only takes a few steps to convert your RecyclerView to use ListAdapter. Now your application can automatically get better performance and a better user experience by using ListAdapter to update only those items that have changed." />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>


    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/delete_button"
        android:layout_width="40dp"
        android:layout_height="match_parent"
        app:srcCompat="@android:drawable/ic_delete" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/move_down_button"
        android:layout_width="40dp"
        android:layout_height="match_parent"
        app:srcCompat="@android:drawable/arrow_down_float" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/info_button"
        android:layout_width="40dp"
        android:layout_height="match_parent"
        app:srcCompat="@android:drawable/ic_menu_info_details" />

    <androidx.appcompat.widget.AppCompatImageView
        android:clickable="true"
        android:id="@+id/move_button"
        android:layout_width="40dp"
        android:layout_height="match_parent"
        app:srcCompat="@drawable/ic_hamburger_menu_bottom_bar" />

</LinearLayout>


